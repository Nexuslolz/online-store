import { IProduct } from '../models/models';

export const boxFilter = (array: IProduct[], checkbox: string, param: string) => {
  // return array.filter((product) => product[param] === checkbox && product);
};
